type Transaction {
  id: ID!
  user: UserProfile
  type: TransactionType   # Enum: ["CONTRIBUTION", "PAYOUT", "DONATION"]
  amount: Float
  createdAt: String
}

enum TransactionType {
  CONTRIBUTION
  PAYOUT
  DONATION
}
